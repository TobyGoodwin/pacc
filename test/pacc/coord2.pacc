/*
type int
parse x 1
parse xx 2
parse xxx 3
parse xxxx 4
*/

/* it's a bit contrived, but this does actually manage to exercise the
 * memmove() code in _pacc_coords()
 */

S ← a:X X X d:X → { a[1] * d[1] }
  / a:X X c:X → { a[1] * c[1] }
  / a:X b:X → { a[1] * b[1] }
  / a:X → {a[1]}

X :: int * ← &{ !!PACC_COORDS } "x" → { PACC_COORDS }
